import { StyleSheet, Text, View, Pressable, SafeAreaView, TextInput, FlatList} from 'react-native';

export default function CreatePlanScreen() {
  return (
    <SafeAreaView>
      <Text>Create plans here</Text>
    </SafeAreaView>
  )
}
